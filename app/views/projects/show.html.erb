<% provide(:title, 'Project') %>

<h1><%= @project.title %></h1>

<div class="row">
  <%= render 'projects/display', project: @project, link: @project.link, size: :large, col_size: "col-md-6 col-md-offset-3", description: @project.description, img_type: 'img-responsive img-rounded' %>
</div>