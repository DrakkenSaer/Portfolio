<% provide(:title, 'Work history') %>

<h1><%= @job.title %></h1>

<div class="row">
  <%= render 'jobs/display', job: @job, col_size: "col-md-6 col-md-offset-3", description: @job.description %>
</div>