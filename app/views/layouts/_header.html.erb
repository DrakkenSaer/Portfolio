<header class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container">
    <nav>
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to "Work in Progress", '#', class: "navbar-brand" %>
      </div>
      <div class="collapse navbar-collapse" id="navbar-collapse">
        <ul class="nav navbar-nav navbar-right">
          <li><%= header_link('Home', root_path) %></li>
          <li><%= header_link('About me', about_path) %></li>
          <li><%= header_link('Contact me', contact_path) %></li>
          <% unless menu_empty?(Job) %>
          <li><%= header_link('Work experience', jobs_path) %></li>
          <% end %>
          <% unless menu_empty?(Project) %>
          <li><%= header_link('Portfolio', projects_path) %></li>
          <% end %>
        </ul>
        </nav>
      </div>
  </div>
</header>